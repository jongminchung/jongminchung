---
id: deep-dive/typescript-7-compatibility
locale: ko
section: deep-dive
title: TypeScript 7 호환성 검증
displayTitle: TypeScript 7 호환성
description: 컴파일러 메이저 업그레이드 전에 라이브러리 선언, Compiler API 결합, 빌드와 도구 체인을 증거로 검증합니다.
order: 4
updatedAt: "2026-07-30"
verifiedAt: "2026-07-30"
tags: [typescript, compatibility, migration, compiler, tooling]
status: stable
sourceUrl: https://devblogs.microsoft.com/typescript/announcing-typescript-7-0/
packageVersion: "7.0.2"
---

TypeScript 메이저 업그레이드는 `tsc --noEmit` 한 번으로 승인할 수 없다. 타입 선언만 소비하는
라이브러리와 Compiler API를 실행하는 도구는 실패 방식이 다르고, 프레임워크가 자체 타입 검사를
실행하면 프로젝트의 `tsc` 성공과 실제 빌드 결과도 달라질 수 있다.

이 절차의 목표는 “아마 호환될 것”을 “어떤 버전과 경계에서 무엇을 실행해 확인했다”로 바꾸는
것이다.

## 1. 정확한 인벤토리를 고정한다

모든 workspace manifest와 lockfile에서 직접 의존성의 정확한 버전을 수집한다. 직접 의존성은
빠짐없이 판정하고, 전이 의존성은 TypeScript peer를 선언하거나 Compiler API를 import하는
패키지까지 추적한다.

```sh
pnpm list -r --depth 0
rg '"typescript"' package.json pnpm-workspace.yaml apps packages
rg 'typescript/lib/typescript|require\\(.typescript.|from .typescript.' node_modules/.pnpm
```

패키지 이름만 기록하지 않는다. 버전, 사용하는 workspace, production/dev 구분, 실행되는
스크립트를 함께 남겨야 같은 증거를 다시 만들 수 있다. lockfile이 바뀌면 검증 대상도 바뀐다.

## 2. 결합 유형을 먼저 분류한다

의존성을 다음 경계로 나누면 필요한 검사가 명확해진다.

| 결합 유형         | 확인할 내용                                                                |
| ----------------- | -------------------------------------------------------------------------- |
| 타입 선언 소비    | TS 7이 실제 import와 공개 타입을 검사하는가                                |
| CLI 실행          | `tsc`, build, test 명령이 TS 7 바이너리로 동작하는가                       |
| Compiler API 소비 | `createProgram`, `readConfigFile`, AST·language service API를 import하는가 |
| embedded language | MDX, Vue, Astro, Svelte 같은 별도 language tooling을 사용하는가            |
| runtime/native    | Node/Electron ABI, packaging, 실제 실행이 정상인가                         |

`peerDependencies.typescript`가 없다는 사실은 지원 선언이 아니다. 반대로 peer 범위에 7이 있어도
사용 중인 API 경로와 빌드를 실행하지 않았다면 검증은 끝나지 않는다.

## 3. 공식 근거와 로컬 증거를 분리한다

먼저 버전이 고정된 공식 문서, release note, package manifest와 소스에서 지원 범위를 찾는다.
그다음 현재 저장소의 typecheck, build, test 결과를 별도 열에 기록한다. 로컬 성공을 제조사의
공식 지원 선언으로 표현하지 않는다.

TypeScript 7.0은 [공식 발표](https://devblogs.microsoft.com/typescript/announcing-typescript-7-0/)에서
안정적인 Compiler API가 없다고 명시한다. 또한 MDX 같은 embedded-language workflow는 당분간
TypeScript 6을 유지하도록 안내한다. 이 차이는 일반적인 `.ts` 소스 호환성과 도구 체인
호환성을 분리해야 하는 이유다.

## 4. TypeScript 6과 7을 나란히 실행한다

API 소비자가 있으면 전역 교체부터 하지 않는다. 추적 파일을 바꾸지 않는 일회용 복사본이나
격리 브랜치에서 기준선, 단일 TS 7, 듀얼 구성 세 가지를 비교한다.

[Nx 공식 병행 절차](https://nx.dev/docs/kb/typescript-7)는 TypeScript 6 API와 TypeScript 7
CLI를 alias로 분리한다.

```json
{
  "devDependencies": {
    "@typescript/native": "npm:typescript@7.0.2",
    "typescript": "npm:@typescript/typescript6@6.0.2"
  }
}
```

```sh
pnpm exec tsc --version
pnpm exec tsc6 --version
node -e "const ts=require('typescript'); console.log(typeof ts.createProgram)"
```

`tsc`는 TS 7 CLI를, `typescript` import는 TS 6 API를 제공해야 한다. 각 workspace에 별도
TypeScript가 설치되어 있다면 어느 패키지가 해석되는지도 함께 기록한다.

## 5. 설정 변경을 먼저 드러낸다

TypeScript 6에서 임시 `ignoreDeprecations`를 제거하고 TS 7의 기본값과 금지된 옵션을 확인한다.
특히 `rootDir`, `types`, `module`, `moduleResolution`, `strict`,
`noUncheckedSideEffectImports`, `libReplacement`을 실제 `--showConfig` 결과로 비교한다.

```sh
pnpm exec tsc --showConfig -p tsconfig.json
pnpm exec tsc --noEmit --incremental false -p tsconfig.json
```

모든 tsconfig를 개별 실행한다. 루트 검사 하나가 앱, Electron main process, 패키지 build
config를 포함한다고 가정하지 않는다.

## 6. 검증 사다리를 끝까지 오른다

낮은 비용의 검사부터 실제 배포 경계까지 순서대로 실행한다.

1. 모든 tsconfig의 `tsc --noEmit`
2. `skipLibCheck: false` 진단
3. bundler와 declaration emit
4. unit/integration/E2E test
5. 프레임워크 자체 타입 검사와 production build
6. watch와 editor/LSP 기능
7. Electron/native packaging과 smoke test

`skipLibCheck: false`는 좋은 감사 도구지만 항상 release gate인 것은 아니다. TS 6에서도 같은
오류가 발생하는지 비교해 기존 선언 부채와 TS 7 회귀를 분리한다. 평소 `skipLibCheck: true`인
프로젝트에서 TS 7 typecheck가 성공했다는 사실만으로 전이 선언 전체가 호환된다고 결론 내리지
않는다.

Next.js 16.2.10은
[검증 코드](https://github.com/vercel/next.js/blob/v16.2.10/packages/next/src/lib/verify-typescript-setup.ts)에서
`typescript/lib/typescript.js`를 찾고,
[타입 검사 구현](https://github.com/vercel/next.js/blob/v16.2.10/packages/next/src/lib/typescript/runTypeCheck.ts)에서
`createProgram`과 `getPreEmitDiagnostics`를 호출한다. 따라서 외부 TS 7 CLI 검사 성공은
`next build` 성공을 대신하지 않는다.

## 7. 판정과 재확인 조건을 함께 남긴다

| 판정                 | 사용 기준                                       |
| -------------------- | ----------------------------------------------- |
| 지원 확인            | 공식 범위 또는 실제 사용 경계가 TS 7에서 통과   |
| 조건부 지원          | TS 6 API, alias, 특정 설정이나 격리가 필요      |
| 차단                 | 단일 TS 7에서 사용 경계의 실패를 재현           |
| TypeScript 비결합    | Compiler API나 TypeScript peer와 무관           |
| 기준선 오류로 미확정 | 기존 실패 때문에 TS 7 고유 회귀를 분리하지 못함 |

조건부·차단 판정에는 “어떤 릴리스나 테스트가 통과하면 다시 볼 것인지”를 반드시 적는다.
검증일, 정확한 버전, 명령, 종료 코드와 첫 번째 원인 오류를 함께 보관한다.

## 8. 롤백 가능한 순서로 배포한다

먼저 대표 패키지에서 TS 7 CLI를 실행하고, API 소비자는 TypeScript 6에 둔다. 이후 affected
build/test, 전체 CI, editor와 watch, production packaging 순으로 범위를 넓힌다. lockfile과
compiler alias는 한 변경으로 검토하고, 실패하면 기존 TypeScript 6 해석 경로로 바로 돌아갈 수
있어야 한다.

이 저장소에서는 단일 TS 7 구성이 Nx graph, Next.js와 콘텐츠 Compiler API를 차단했다.
듀얼 구성에서는 Nx graph와 TS 7 CLI를 함께 실행할 수 있었지만, 실제 manifest 전환은 별도
변경과 전체 회귀 검증이 필요하다. 현재 결론은 전체 일괄 전환이 아니라 역할별 병행이다.

## 참고 자료

- [TypeScript 7.0 발표](https://devblogs.microsoft.com/typescript/announcing-typescript-7-0/)
- [Nx의 TypeScript 지원 범위](https://nx.dev/docs/technologies/typescript/introduction)
- [Nx TypeScript 6/7 병행 절차](https://nx.dev/docs/kb/typescript-7)
- [Next.js 16.2.10 TypeScript setup](https://github.com/vercel/next.js/blob/v16.2.10/packages/next/src/lib/verify-typescript-setup.ts)
- [저장소 호환성 감사 보고서](https://github.com/jongminchung/jongminchung/blob/main/docs/typescript-7-compatibility-report.md)
