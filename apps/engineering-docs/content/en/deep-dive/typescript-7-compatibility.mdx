---
id: deep-dive/typescript-7-compatibility
locale: en
section: deep-dive
title: TypeScript 7 Compatibility Verification
displayTitle: TypeScript 7 Compatibility
description: Verify library declarations, Compiler API coupling, builds, and tooling before a compiler major upgrade.
order: 4
updatedAt: "2026-07-30"
verifiedAt: "2026-07-30"
tags: [typescript, compatibility, migration, compiler, tooling]
status: stable
sourceUrl: https://devblogs.microsoft.com/typescript/announcing-typescript-7-0/
packageVersion: "7.0.2"
---

A TypeScript major upgrade cannot be approved with one `tsc --noEmit` run. A library that only
publishes declarations fails differently from a tool that executes the Compiler API. A framework
may also run its own type checker, making a successful project `tsc` run different from a successful
production build.

This procedure replaces "it is probably compatible" with "we ran this evidence at this version and
boundary."

## 1. Freeze an exact inventory

Collect exact direct dependency versions from every workspace manifest and the lockfile. Decide on
every direct dependency, then follow transitive packages that declare a TypeScript peer or import
the Compiler API.

```sh
pnpm list -r --depth 0
rg '"typescript"' package.json pnpm-workspace.yaml apps packages
rg 'typescript/lib/typescript|require\\(.typescript.|from .typescript.' node_modules/.pnpm
```

Record more than package names: include the version, consuming workspace, production or development
role, and scripts that execute it. A lockfile change creates a new verification target.

## 2. Classify the coupling first

The required evidence becomes clearer when dependencies are divided by boundary.

| Coupling              | What to verify                                                                |
| --------------------- | ----------------------------------------------------------------------------- |
| Declaration consumer  | TS 7 checks real imports and public types                                     |
| CLI execution         | `tsc`, build, and test commands execute with the TS 7 binary                  |
| Compiler API consumer | Code imports `createProgram`, `readConfigFile`, AST, or language-service APIs |
| Embedded language     | MDX, Vue, Astro, or Svelte uses separate language tooling                     |
| Runtime or native     | Node/Electron ABI, packaging, and real execution still work                   |

The absence of `peerDependencies.typescript` is not a support statement. Conversely, a peer range
that includes 7 is insufficient until the API path and build used by the repository have run.

## 3. Separate official claims from local evidence

Start with versioned official documentation, release notes, published manifests, and source. Record
repository typecheck, build, and test results in a separate evidence column. Do not present a local
success as a vendor support commitment.

The [TypeScript 7 announcement](https://devblogs.microsoft.com/typescript/announcing-typescript-7-0/)
states that 7.0 has no stable Compiler API. It also advises embedded-language workflows such as MDX
to remain on TypeScript 6 for now. That distinction is why source compatibility and toolchain
compatibility require separate decisions.

## 4. Run TypeScript 6 and 7 side by side

Do not begin with a global replacement when an API consumer exists. Compare the baseline, uniform
TS 7, and dual-compiler configurations in a disposable copy or isolated branch.

The [official Nx side-by-side procedure](https://nx.dev/docs/kb/typescript-7) separates the
TypeScript 6 API from the TypeScript 7 CLI.

```json
{
  "devDependencies": {
    "@typescript/native": "npm:typescript@7.0.2",
    "typescript": "npm:@typescript/typescript6@6.0.2"
  }
}
```

```sh
pnpm exec tsc --version
pnpm exec tsc6 --version
node -e "const ts=require('typescript'); console.log(typeof ts.createProgram)"
```

`tsc` should expose the TS 7 CLI, while an import of `typescript` should expose the TS 6 API. If
workspaces install their own TypeScript versions, record which package each command and import
resolves.

## 5. Surface configuration changes first

Remove temporary `ignoreDeprecations` settings on TypeScript 6, then inspect the new defaults and
removed options in TypeScript 7. Compare the effective values of `rootDir`, `types`, `module`,
`moduleResolution`, `strict`, `noUncheckedSideEffectImports`, and `libReplacement`.

```sh
pnpm exec tsc --showConfig -p tsconfig.json
pnpm exec tsc --noEmit --incremental false -p tsconfig.json
```

Run every tsconfig explicitly. A root check does not necessarily include application, Electron main
process, package build, or test configurations.

## 6. Complete the verification ladder

Move from inexpensive checks to the actual delivery boundary.

1. `tsc --noEmit` for every tsconfig
2. A diagnostic run with `skipLibCheck: false`
3. Bundler and declaration emit
4. Unit, integration, and E2E tests
5. Framework-owned type checking and production builds
6. Watch mode and editor/LSP behavior
7. Electron/native packaging and smoke tests

`skipLibCheck: false` is a useful audit, but it does not have to be the normal release gate. Compare
the same failures on TypeScript 6 to separate existing declaration debt from a TS 7 regression. A
successful TS 7 check in a project that normally uses `skipLibCheck: true` does not prove that every
transitive declaration is compatible.

Next.js 16.2.10 looks for `typescript/lib/typescript.js` in its
[setup verification](https://github.com/vercel/next.js/blob/v16.2.10/packages/next/src/lib/verify-typescript-setup.ts)
and calls `createProgram` and `getPreEmitDiagnostics` in
[its type checker](https://github.com/vercel/next.js/blob/v16.2.10/packages/next/src/lib/typescript/runTypeCheck.ts).
An external TS 7 CLI success therefore does not replace a `next build`.

## 7. Record a decision and a recheck trigger

| Decision               | When to use it                                                      |
| ---------------------- | ------------------------------------------------------------------- |
| Verified               | An official range or the actual usage boundary passes on TS 7       |
| Conditional            | A TS 6 API, alias, specific setting, or isolation remains necessary |
| Blocked                | The usage boundary fails in a uniform TS 7 configuration            |
| TypeScript-independent | No Compiler API or TypeScript peer coupling exists                  |
| Inconclusive baseline  | Existing failures prevent isolating a TS 7 regression               |

Every conditional or blocked decision needs a recheck trigger: name the release or test that would
change the result. Preserve the verification date, exact versions, command, exit code, and first
causal error.

## 8. Roll out in a reversible order

Start by running the TS 7 CLI on a representative package while API consumers stay on TypeScript 6.
Expand through affected builds and tests, full CI, editor and watch behavior, and production
packaging. Review the lockfile and compiler aliases together, and keep a direct path back to the
previous TypeScript 6 resolution.

In this repository, a uniform TS 7 setup blocked the Nx graph, Next.js, and the content Compiler API.
The dual setup allowed the Nx graph and TS 7 CLI to coexist, but changing real manifests still
requires a separate change and full regression run. The current decision is role-based coexistence,
not a uniform upgrade.

## References

- [Announcing TypeScript 7.0](https://devblogs.microsoft.com/typescript/announcing-typescript-7-0/)
- [Nx TypeScript support matrix](https://nx.dev/docs/technologies/typescript/introduction)
- [Nx: use TypeScript 7 alongside TypeScript 6](https://nx.dev/docs/kb/typescript-7)
- [Next.js 16.2.10 TypeScript setup](https://github.com/vercel/next.js/blob/v16.2.10/packages/next/src/lib/verify-typescript-setup.ts)
- [Repository compatibility audit](https://github.com/jongminchung/jongminchung/blob/main/docs/typescript-7-compatibility-report.md)
