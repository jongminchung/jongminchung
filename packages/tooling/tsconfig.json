{
  // 배포 산출물 대신 workspace 소스와 테스트를 검사하는 개발용 설정임.
  "extends": "../../tsconfig.base.json",
  "compilerOptions": {
    // Bun 테스트와 Node API에 필요한 전역 타입을 포함함.
    "types": ["bun", "node"]
  },
  "include": ["src/**/*.ts"],
  "exclude": ["node_modules", "dist"]
}
